Debug apache 2 :
$ tail -f /var/log/apache2/access.log
$ tail -f /var/log/apache2/ error.log

router :
	verfi module
	verif accessfile in apache.conf
	verfi allowoverride in apache.conf
	tuto digitalocean.com / apache.org
	sudo a2enmod rewrite && sudo service apache2 restart
